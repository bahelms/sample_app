<% provide(:title, 'All Users') %>
<h1>All Users</h1>

<%= will_paginate %><!-- Assumes @users because this renders from Users controller -->

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>